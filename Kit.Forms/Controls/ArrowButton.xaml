<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fonts="clr-namespace:Kit.Forms.Fonts;assembly=Kit.Forms"
             xmlns:controls="clr-namespace:Kit.Forms.Controls;assembly=Kit.Forms"
             x:Class="Kit.Forms.Controls.ArrowButton" 
             x:DataType="controls:ArrowButton" BackgroundColor="WhiteSmoke">
    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Command="{Binding Command}" CommandParameter="{Binding CommandParameter }"/>
    </ContentView.GestureRecognizers>
    <ContentView.Content>
        <Frame Padding="5" BorderColor="LightGray">
            <Grid ColumnDefinitions="Auto,*,Auto">

                <Image HeightRequest="20" Margin="5,0,0,0" Grid.Column="0" VerticalOptions="CenterAndExpand" 
                       Source="{Binding Icon}"/>

                <StackLayout Margin="10,5" Grid.Column="1">
                    <Label Text="{Binding Title}" 
                           TextColor="{Binding TitleColor}"
                           FontSize="{Binding TitleFontSize}"/>

                    <Label Text="{Binding SubTitle}" TextColor="{Binding SubTitleColor}" 
                           FontSize="{Binding SubTitleFontSize}"/>
                </StackLayout>

                <Image Margin="10,0" Grid.Column="2" VerticalOptions="CenterAndExpand">
                    <Image.Source>
                        <FontImageSource Size="30"
                                         Glyph="{x:Static fonts:FontelloIcons.RightArrow}"  
                                         FontFamily="{x:Static fonts:FontelloIcons.Font}" 
                                         Color="{Binding ArrowColor}"/>
                    </Image.Source>
                </Image>
                <Label />
            </Grid>
        </Frame>
    </ContentView.Content>
</ContentView>